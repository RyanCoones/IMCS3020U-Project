import React from "react";
// pick some icons from lucide-react and import later

interface SidebarProps {
    // will need a username, iscollapsed flag, and a function for collapse
}

const Sidebar: React.FC<SidebarProps> = () => {
    
    return(
        <div>
            Sidebar
        </div>
    );

};

export default Sidebar;